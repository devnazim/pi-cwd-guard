{
  "name": "pi-cwd-guard",
  "version": "0.1.0",
  "description": "Pi extension package that asks before read/write/edit tools access files outside the current working directory.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "cwd",
    "guard"
  ],
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "pack:dry-run": "npm pack --dry-run"
  },
  "files": [
    "index.ts",
    "src",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typescript": "^5.0.0"
  }
}
